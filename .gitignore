output/
.agents/
.claude/
skills-lock.json
.env

# Your own contact details, education, publications (ADR-0037). The repo ships
# data/profile.example.yaml; copy it to data/profile.yaml and it stays local.
data/profile.yaml

# personal tracked data (ADR-0008: flat files, not meant to be version-controlled)
data/jobs/
data/applications/
data/usage-log.json
data/usage-log.incomplete.json

# site (Astro landing page + docs)
site/node_modules/
site/dist/
site/.astro/

# frontend
frontend/node_modules/
frontend/dist/

# extension (test tooling only — the extension itself ships unbundled)
extension/node_modules/

# backend
backend/server

# site (Astro landing page + docs)
site/node_modules/
site/dist/
site/.astro/
